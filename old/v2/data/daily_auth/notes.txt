Open points
 - sesisons saved to local files. move to some cache db.
  - (stonks) C:\Machine Learning\stonks\data\daily_auth>uv run trigger.py # to start auth
  - Kill the redirect to /data_fetch if /daily_auth accessed with valid access token : {kept open for testing}

To Remember:
 - Start sessions_access_token to have a dummy access_token json. 
 Add a dummy json object if you have to create this file in trigger.py

 Remarks:
  - The nse api is not reliable, blocks requests a lot. Use it for about 250EQs for now then move to Kite

Daily auth
 - Validates KITE API key,
  - Uses LLM to automatically input OTP on the browser
 - Fetches:
  - Portfolio holdings,
  - Portfolio orders,
  - Daily quotes for full market

Market Daily Holdings:
 - Use yfinance for one-time historical data fill
 - Use Kite API quote endpoint to fetch daily once daily auth is set up


Portfolio
 - store portoflio states